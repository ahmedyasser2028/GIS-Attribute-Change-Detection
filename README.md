# GIS Attribute Comparison Tool

Python script for comparing attribute values between two GIS layers using ArcPy.

## Features

- Detect attribute differences
- Identify missing records
- Export differences to a table

## Requirements

- ArcGIS Pro
- Python (ArcPy)

## Usage

Update paths in the script then run:

python compare_layers.py
